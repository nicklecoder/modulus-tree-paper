
\documentclass[final]{lib/aomartmod}
% change class to "manuscript" to bring back line numbers
% \documentclass[10pt]{article}
\usepackage{amsmath}
\usepackage{amsfonts}

\usepackage{graphicx}

\usepackage{tikz}
\usepackage{algorithmic}
\usepackage{subfig}

\usepackage{array} %table tools

\usepackage{url} %for urls in pdf's

% correct bad hyphenation here
\hyphenation{op-tical net-works semi-conduc-tor}

% custom includes
\usepackage{newclude} % to prevent \clearpage on \include*{filename}
                      % May need to be included in main.tex

% custom structure types (theorems, definitions, lemmas, etc)
% \usepackage{amsthm}
\newtheorem{theorem}{Theorem}
\newtheorem{definition}{Definition}
\newtheorem{lemma}{Lemma}
